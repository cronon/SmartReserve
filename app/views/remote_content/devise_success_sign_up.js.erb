$('#sign_up_modal.modal').modal('hide');
$('#succes_modal').modal();
location.reload(false);

// if($('#flash').length > 0) {
//  $('#flash').remove();
//  $('body').prepend("<%= escape_javascript raw(flash_normal) %>");
// }
// else {
//  $('.modal').modal('hide')
//  $('body').prepend("<%= escape_javascript raw(flash_normal) %>");
// }