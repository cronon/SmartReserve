<% if user_signed_in? %>
    <%= link_to t("Hello, #{current_user.email}"), user_root_path,class: 'navbar-brand' %>
    <%= link_to t('log out'), destroy_user_session_path, :method => 'delete' ,class: 'navbar-brand'%>
<% else %>
    <%= link_to t('Sign in'), new_user_session_path, class: 'navbar-brand' %> 
    <%= link_to t('Sign up'), new_user_registration_path, class: 'navbar-brand' %>
<% end %>