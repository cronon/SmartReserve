<%= form_for(@order, url: club_order_create_path, remote: true) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">

      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>

    </div>
  <% end %>

  <%= f.hidden_field :table_id %>

  <div class="field">
    <%= f.label :time %><br>

    <!-- не f.select_datetime не просто так-->
    <%= select_datetime Time.now, prefix: :time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>
