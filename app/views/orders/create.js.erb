<% if @order.errors.any? %>
  $('#confirmation_dialog').html("<%= j(render(partial:'confirmation', object: @order)) %>");
<% else %>
  $('#confirmation_dialog').modal('hide')
  window.location.replace("<%=club_order_url(@order.table.club, @order)%>");
<% end %>