<h1>Listing clubs</h1>
<% @clubs.each_slice(3) do |three_clubs| %>
  <div class="row-fluid">
    <% three_clubs.each do |club| %>
      <% @club = club %>
      <%= render 'clubs/single' %>
    <% end %>
  </div>    
<% end %>

<br>

<%= link_to 'New Club', new_club_path %>
